This is a React + TypeScript project powered by [Vite](https://vitejs.dev).

## Getting Started

First, run the development server:

```bash
npm run dev
```

Then open `http://localhost:5173` in your browser to see the app.

You can start editing the UI by modifying `src/App.tsx`. The page auto-updates as you edit the file.
